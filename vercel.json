{
  "buildCommand": "npm run build --workspace=client && npm run build --workspace=server && npx prisma generate --schema=server/prisma/schema.prisma",
  "outputDirectory": "client/dist",
  "functions": {
    "api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
